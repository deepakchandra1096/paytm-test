apiVersion: v1
kind: Service
metadata:
  name: Applicaion_Name
  labels:
    run: Applicaion_Name
spec:
  type: ClusterIP
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: Applicaion_Name
